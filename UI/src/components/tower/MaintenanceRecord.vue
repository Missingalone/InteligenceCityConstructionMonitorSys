<script setup lang="ts">import type { MaintenanceRecord } from '@/api/towerTypes';defineProps<{records:MaintenanceRecord[];loading?:boolean}>()</script>
<template><div v-if="loading" class="record-state">维护记录加载中...</div><div v-else-if="!records.length" class="record-state">暂无维护记录</div><div v-else class="records"><div v-for="item in records" :key="item.id" class="record"><span class="type">{{ item.category }}</span><div><strong>{{ item.content }}</strong><p>检查人员：{{ item.inspector }} · 检查日期：{{ item.date }} · 结果：<b>{{ item.result }}</b></p><p>下次维护：{{ item.nextDate }} · 检验报告：{{ item.reportStatus }}</p></div></div></div></template>
<style scoped>.records{display:grid;gap:8px}.record{display:grid;grid-template-columns:80px 1fr;gap:10px;padding:10px;border:1px solid rgba(79,168,255,.13);border-radius:5px;background:rgba(9,32,57,.52)}.type{align-self:start;padding:5px;border-radius:4px;color:#59b2ef;background:rgba(22,119,255,.13);font-size:9px;text-align:center}.record strong{color:#d3e5f4;font-size:11px}.record p{margin:6px 0 0;color:#758faa;font-size:9px}.record p b{color:#39d7a2}.record-state{display:grid;min-height:180px;place-items:center;color:#6d8ca8}</style>

