<script setup lang="ts">import { computed } from 'vue';import { VideoOff } from 'lucide-vue-next';import type { CameraRecord } from '@/api/camera';const props=defineProps<{modelValue:boolean;camera?:CameraRecord}>();const emit=defineEmits<{ 'update:modelValue':[v:boolean] }>();const visible=computed({get:()=>props.modelValue,set:v=>emit('update:modelValue',v)})</script>
<template><el-dialog v-model="visible" title="实时视频" width="min(900px,94vw)"><div class="video-placeholder"><VideoOff :size="38"/><strong>{{ camera?.cameraName||'未选择摄像头' }}</strong><span>{{ camera?.status===1?'视频流接口尚未提供，已保留播放器接入位置':'摄像头当前离线' }}</span><small>{{ camera?.cameraCode }} · {{ camera?.installationAddress }}</small></div></el-dialog></template>
<style scoped>.video-placeholder{display:flex;align-items:center;justify-content:center;flex-direction:column;gap:9px;height:min(55vh,500px);border:1px solid rgba(79,168,255,.2);border-radius:7px;color:#6486a3;background:radial-gradient(circle,#103557,#040e19)}.video-placeholder strong{color:#d7eafa}.video-placeholder small{color:#4e7391}</style>

